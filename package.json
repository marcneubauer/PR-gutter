{
  "name": "pr-gutter",
  "displayName": "PR Gutter",
  "description": "Shows git diff highlighting in the gutter between current branch and a configurable target branch",
  "version": "0.0.1",
  "publisher": "marcneubauer",
  "repository": {
    "type": "git",
    "url": "https://github.com/marcneubauer/pr-gutter.git"
  },
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "pr-gutter.setTargetBranch",
        "title": "Set Target Branch",
        "category": "PR Gutter"
      },
      {
        "command": "pr-gutter.refreshDiff",
        "title": "Refresh Diff",
        "category": "PR Gutter"
      },
      {
        "command": "pr-gutter.showDebugInfo",
        "title": "Show Debug Info",
        "category": "PR Gutter"
      }
    ],
    "configuration": {
      "title": "PR Gutter",
      "properties": {
        "pr-gutter.targetBranch": {
          "type": "string",
          "default": "main",
          "description": "The target branch to compare against (ignored if targetCommit is set)"
        },
        "pr-gutter.targetCommit": {
          "type": "string",
          "default": "",
          "description": "The target commit hash to compare against (takes precedence over targetBranch if set)"
        },
        "pr-gutter.autoRefresh": {
          "type": "boolean",
          "default": true,
          "description": "Automatically refresh diff when files change"
        },
        "pr-gutter.showOutline": {
          "type": "boolean",
          "default": true,
          "description": "Show border outline around changed lines"
        },
        "pr-gutter.showGutterIcons": {
          "type": "boolean",
          "default": true,
          "description": "Show emoji icons in the gutter for changed lines"
        },
        "pr-gutter.showStartupNotification": {
          "type": "boolean",
          "default": true,
          "description": "Show a notification when the extension activates"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "webpack",
    "watch": "webpack --watch",
    "package": "webpack --mode production --devtool hidden-source-map",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "reinstall": "npm run package && npx vsce package --allow-missing-repository && code --uninstall-extension marcneubauer.pr-gutter ; code --install-extension /Users/marc/Documents/code/PR-gutter/pr-gutter-0.0.1.vsix"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "@types/vscode": "^1.74.0",
    "@vscode/vsce": "^3.7.0",
    "ts-loader": "^9.5.4",
    "typescript": "^5.9.3",
    "webpack": "^5.102.1",
    "webpack-cli": "^6.0.1"
  },
  "dependencies": {
    "simple-git": "^3.30.0"
  }
}
